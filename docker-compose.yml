version: "3.9"

services:
  app:
    build: .
    container_name: agonarena-bot-app
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
