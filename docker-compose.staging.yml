version: "3.9"

services:
  app:
    build: .
    container_name: agonarena-bot-staging
    env_file:
      - .env.staging
    ports:
      - "8081:8080"
    volumes:
      - ./data/staging:/app/data
    restart: unless-stopped
